@page "/addinventory"

@inject IAddInventoryItemService AddInventoryItemService
@inject NavigationManager NavigationManager

<h1>Add Inventory</h1>

<AddEditInventoryItem InventoryPopulated="@InventoryPopulated" 
                       operation="@operation" />

@code {
    public Inventory InventoryPopulated = new()
        {
            InventoryName = "",
            Quantity = 1,
            Price = 1
        };

    public string operation = "add";
}
